import styled from 'styled-components';
import { StyledStandardSpan } from './StyledStandardSpan';

export const StyledSummaryPriceSpan = styled(StyledStandardSpan)`
  font-size: 16px;
  font-weight: bold;
`