import styled from 'styled-components';
import { StyledStandardSpan } from './StyledStandardSpan';

export const StyledSummaryTotalCost = styled(StyledStandardSpan)`
  flex-basis: 100%;
  text-transform: uppercase;
`